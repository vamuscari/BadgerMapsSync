# BadgerMaps CLI Environment Variables Example
# Copy this file to .env and update the values as needed

# Database Configuration
DB_TYPE=sqlite3          # Options: postgres, mssql, sqlite3
DB_HOST=localhost        # Database host (not used for sqlite3)
DB_PORT=5432            # Database port (not used for sqlite3)
DB_NAME=badgersync.db   # Database name (filename for sqlite3)
DB_USER=                # Database username (not used for sqlite3)
DB_PASSWORD=            # Database password (not used for sqlite3)

# BadgerMaps API Configuration
BADGERMAPS_API_KEY=your_api_key_here
BADGERMAPS_API_URL=https://badgerapis.badgermapping.com/api/2

# Sync Configuration
SYNC_INTERVAL=3600      # Sync interval in seconds (1 hour)
BATCH_SIZE=1000         # Number of records to process in each batch
ENABLE_AUTO_SYNC=true   # Enable automatic syncing

# Logging Configuration
LOG_LEVEL=info          # Options: debug, info, warn, error
LOG_FILE=./badgersync.log  # Leave empty for console-only logging

# Example configurations for different database types:

# PostgreSQL Example:
# DB_TYPE=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=badgermaps
# DB_USER=badger_user
# DB_PASSWORD=your_secure_password

# MSSQL Example:
# DB_TYPE=mssql
# DB_HOST=localhost
# DB_PORT=1433
# DB_NAME=badgermaps
# DB_USER=badger_user
# DB_PASSWORD=your_secure_password

# SQLite3 Example (default):
# DB_TYPE=sqlite3
# DB_NAME=badgersync.db
# (other database fields not used) 